// hypercomb-core/src/index.ts
//
// `@hypercomb/core` is the processor (processor.ts) plus the library
// (library.ts). The surface is one; the pure install ships the processor and
// resolves the library by signature.

export * from './processor.js'
export * from './library.js'

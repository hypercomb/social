{
  "name": "hypercomb-web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "ng serve",
    "build": "ng build",
    "runtime": "npm run runtime:pixi",
    "runtime:pixi": "npx tsx ./scripts/build-pixi-vendor.ts"
  },
  "dependencies": {
    "@hypercomb/core": "file:../hypercomb-core"
  },
  "devDependencies": {
    "pixi.js": "^8.16.0"
  }
}

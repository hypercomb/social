# auto-generated by prepare script
src/**/index.ts
src/**/*-keys.ts

# build output
# dist/     — build-module.ts: signature-addressed package (sig files + manifest.json)
# dist-lib/ — tsup: the library bundle. Kept apart so neither clean wipes the other.
dist/
dist-lib/

# build cache (local optimization, not shared)
.build-cache.json

public/contet/
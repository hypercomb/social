// commands/publications-kind.ts
//
// THE PUBLICATIONS VIEW'S NAMES — its view and its decoration kind. The
// /publications word and the view that renders it both read these, and a bee
// is never imported for a value (atomic-modules-plan.md), so they live here,
// in a dependency both import.

export const PUBLICATIONS_VIEW = 'publications'

export const PUBLICATIONS_KIND = 'visual:publications:view'

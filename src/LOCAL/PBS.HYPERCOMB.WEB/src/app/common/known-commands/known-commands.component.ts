import { Component } from '@angular/core'
// import { KNOWN_COMMANDS } from 'src/app/shortcuts/known-config';

@Component({
  selector: 'app-known-commands',
  imports: [],
  templateUrl: './known-commands.component.html',
  styleUrl: './known-commands.component.scss'
})
export class KnownCommandsComponent {
  // public readonly commands = Array.from(KNOWN_COMMANDS).sort()
}


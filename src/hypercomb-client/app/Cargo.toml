[package]
name = "hypercomb-client"
description = "Hypercomb — a native window on your hive."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
hypercomb-host.workspace = true
hypercomb-serve.workspace = true
hypercomb-store.workspace = true
# `unstable` is multi-webview: on Windows the window is one title-bar webview
# over the content webview (see `titlebar` in main.rs).
tauri = { version = "2", features = ["unstable"] }
tauri-plugin-dialog = "2"
tauri-plugin-deep-link = { version = "2", optional = true }
tauri-plugin-single-instance = { version = "2", features = ["deep-link"], optional = true }
serde.workspace = true
serde_json.workspace = true

[features]
# Enabled by `tauri dev`/`tauri build`; keeps the custom protocol available.
custom-protocol = ["tauri/custom-protocol"]
minimal-deep-link = ["dep:tauri-plugin-deep-link", "dep:tauri-plugin-single-instance"]

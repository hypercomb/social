// hypercomb-shared/core/i18n.service.ts
//
// MIGRATION STUB — see the note in store.ts. LocalizationService moved to
// `@hypercomb/runtime`: it imports nothing but @hypercomb/core, every UI
// surface reaches it through IoC rather than by import, and a host needs it as
// much as a shell does.
export * from '@hypercomb/runtime/i18n.service'

// hypercomb-shared/core/shell-surface-registry.ts
//
// MIGRATION STUB — temporary, and it should get shorter, not longer.
//
// This module moved to `@hypercomb/runtime` so a host can ship without the
// monorepo (documentation/shim-extraction-scope.md). The re-export keeps every
// existing import path working, so web and dev are unchanged by the move.
//
// Do not add anything here. When the last importer says
// `@hypercomb/runtime/shell-surface-registry`, delete this file.
export * from '@hypercomb/runtime/shell-surface-registry'
